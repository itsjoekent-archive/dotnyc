import './index.css';
import './reset.css';

const bioButtons = document.querySelectorAll('[data-bio-button]');
const bioText = document.querySelectorAll('[data-bio-text]');
console.log(bioButtons, bioText);
