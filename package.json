{
  "scripts": {
    "start": "parcel --open --port 5000 src/index.html",
    "build": "parcel build src/index.html",
    "media": "node bin/media.js",
    "content": "cp -R content dist",
    "deploy": "npm run build && npm run media && npm run content && node bin/deploy"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "@parcel/transformer-css": {
    "drafts": {
      "nesting": true
    }
  },
  "devDependencies": {
    "@parcel/transformer-toml": "^2.6.1",
    "parcel": "^2.6.1",
    "prettier": "^2.7.1",
    "process": "^0.11.10"
  },
  "dependencies": {
    "aws-sdk": "^2.1159.0",
    "copy-to-clipboard": "^3.3.1",
    "dotenv": "^16.0.1",
    "download": "^8.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-markdown": "^8.0.3",
    "toml": "^3.0.0",
    "wouter": "^2.8.0-alpha.2"
  }
}
